# Dependencias del notebook de estudiantes (Google Colab T4)
# Versiones mínimas conocidas que cargan Qwen2.5-1.5B-Instruct sin cambios.
# Tras la prueba final del taller, regenera con:
#   pip freeze | grep -E '^(transformers|accelerate|bitsandbytes|torch|openai|pandas|matplotlib|seaborn)=' > requirements.lock.txt

transformers>=4.45.0,<5.0
accelerate>=0.34.0
bitsandbytes>=0.43.0
torch>=2.4.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.13.0
openai>=1.50.0
